# Cloudflare Worker — Asystent zakupowy farbyjachtowe.pl
# Dokumentacja: https://developers.cloudflare.com/workers/

name = "asystent-farbyjachtowe"
main = "worker.js"
compatibility_date = "2024-09-23"

# ─── SEKRET (NIE wpisuj klucza tutaj!) ────────────────────────────────────────
# Ustaw przez terminal przed wdrożeniem:
#
#   wrangler secret put ANTHROPIC_API_KEY
#   (wklej klucz z https://console.anthropic.com/settings/keys)
#
# To jedyna rzecz do skonfigurowania. Katalog produktów jest wbudowany
# w products.js — żeby go zaktualizować, przegeneruj plik z nowego CSV
# i ponownie uruchom `wrangler deploy`.
